Perfect 🌿 — let’s make your site support two full themes in Tailwind:

🟠 Primary (Orange) — default

🟢 Secondary (Green) — toggled with a class (e.g., theme-green)

This setup lets you switch between color themes dynamically — perfect for “Eco Mode,” “Light Mode,” or seasonal styles — without changing your Tailwind build.

⚙️ Step 1: Tailwind Config

In your tailwind.config.js, define both themes using CSS variables.
That way, you can toggle themes just by changing variables.

// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: 'rgb(var(--color-primary) / <alpha-value>)',
          foreground: 'rgb(var(--color-primary-foreground) / <alpha-value>)',
        },
        secondary: {
          DEFAULT: 'rgb(var(--color-secondary) / <alpha-value>)',
          foreground: 'rgb(var(--color-secondary-foreground) / <alpha-value>)',
        },
      },
    },
  },
  plugins: [],
}

🎨 Step 2: Define Both Themes in CSS

Create or edit your globals.css (or app.css) file and add both theme definitions.

/* 🌞 Default: Orange Theme */
:root {
  --color-primary: 249 115 22; /* #F97316 */
  --color-primary-foreground: 255 255 255;

  --color-secondary: 235 248 195; /* #EBF8C3 */
  --color-secondary-foreground: 62 79 9; /* darker contrast */
}

/* 🌿 Green Theme */
.theme-green {
  --color-primary: 181 230 32; /* #B5E620 */
  --color-primary-foreground: 36 47 5;

  --color-secondary: 203 237 97; /* #CBED61 */
  --color-secondary-foreground: 46 60 7;
}

🧩 Step 3: Usage in Components

Now you can use the same Tailwind classes everywhere — and the colors will switch automatically when you add the .theme-green class to <html> or <body>.

<!-- Default: Orange Theme -->
<body>
  <button class="bg-primary text-primary-foreground px-4 py-2 rounded">
    Primary Button
  </button>
  <div class="bg-secondary text-secondary-foreground p-4 rounded">
    Secondary Section
  </div>
</body>


When you want to activate the green theme:

<body class="theme-green">
  <button class="bg-primary text-primary-foreground px-4 py-2 rounded">
    Eco Button
  </button>
  <div class="bg-secondary text-secondary-foreground p-4 rounded">
    Eco Section
  </div>
</body>

🪄 Step 4: Optional JS Toggle (if using Next.js / React)

You can easily toggle the theme class:

// themeToggle.js
export function toggleTheme() {
  document.body.classList.toggle('theme-green')
}


Or with a React button:

<button onClick={() => document.body.classList.toggle('theme-green')}>
  Toggle Theme 🌈
</button>

✅ Result

🟠 Default: Orange-based UI

🟢 Add .theme-green → Instantly switches to green tones

💡 Works with Tailwind color utilities (bg-primary, text-secondary-foreground, etc.)

🔁 No rebuild or reload needed — all CSS variable–driven